import styled from "styled-components"
const ScoreboxImage = styled.img`
    border-radius: 50%;
    min-width: 70px;
    height: 70px;  
    object-fit: cover;
`
export const S = {
    ScoreboxImage: ScoreboxImage
}